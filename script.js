// Escribe una función que tome dos números como argumentos y devuelva el mayor de los dos.

let numero = (a, b) => {
    if(a > b){
      return a
    }else 
      return b
  };
  
  console.log(numero(6,3))